name: apk_analyzer
description: APK Static Analyzer App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.1.10

flutter:
  uses-material-design: true
